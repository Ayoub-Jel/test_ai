apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: car-dealership
  labels:
    app: api
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: api